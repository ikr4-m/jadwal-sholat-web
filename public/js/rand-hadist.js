var hadist = new Array(
    // masih kosong
);

var randHadist = Math.floor(Math.random() * hadist.length);
var hadistIntegral = (3 * 60) * 100;
setTimeout(() => {
    //console.log(hadist[randHadist])
}, hadistIntegral);